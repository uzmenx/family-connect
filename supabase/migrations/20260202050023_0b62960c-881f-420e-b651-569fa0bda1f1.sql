-- Add family_invitation type to notifications table
-- Since type is just a text field, we can use 'family_invitation' directly

-- Nothing to change structurally, the notifications table already supports any type string
-- Just documenting the new type: 'family_invitation'

-- No actual schema changes needed since 'type' column is already text